server:
  port: 8080 # ??????

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ????????????????????
    url: jdbc:mysql://localhost:3306/auto_parts_info?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: qwer1234

mybatis:
  # 告诉 MyBatis 去 resources/mapper 文件夹下找所有的 xml 文件
  mapper-locations: classpath:mapper/*.xml # MyBatis XML????
  configuration:
    map-underscore-to-camel-case: true # ?????????? (? parts_name -> partsName)
